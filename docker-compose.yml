services:
  api:
    build: .
    ports:
      - "8001:8001"
    environment:
      - HF_TOKEN=${HF_TOKEN}
    volumes:
      - ./audio_chunks:/app/audio_chunks
      - ./processed_audio:/app/processed_audio
    restart: unless-stopped
